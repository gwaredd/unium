using System;

namespace Unium.Test
{
    class Program
    {
        static void Main( string[] args )
        {
            Console.WriteLine( "Use the Visual Studio test runner to run the tests ..." );
            Console.WriteLine( "  Test -> Run -> All Tests" );
            Console.ReadKey();
        }
    }
}
